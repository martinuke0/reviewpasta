# API Configuration (optional - defaults to /api for same-origin)
# VITE_API_URL=/api

# Admin Dashboard Password (server-side only, set in Cloudflare Workers)
# Set via: wrangler secret put ADMIN_PASSWORD
# ADMIN_PASSWORD=your-secure-password-here
