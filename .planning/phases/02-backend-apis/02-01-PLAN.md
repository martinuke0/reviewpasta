---
phase: 02-backend-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [functions/api/[[path]].ts]
autonomous: true

must_haves:
  truths:
    - "GET /api/businesses accepts pagination parameters"
    - "API returns paginated results with metadata"
    - "Pagination handles edge cases gracefully"
  artifacts:
    - path: "functions/api/[[path]].ts"
      provides: "Paginated business list endpoint"
      min_lines: 200
  key_links:
    - from: "GET /api/businesses"
      to: "D1 database query"
      via: "LIMIT and OFFSET clauses"
      pattern: "LIMIT.*OFFSET"
---

<objective>
Add pagination support to the business list endpoint for scalability

Purpose: Enable efficient retrieval of businesses as the list grows beyond a manageable size
Output: GET /api/businesses with pagination via query parameters
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
@functions/api/[[path]].ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pagination for business list</name>
  <files>functions/api/[[path]].ts</files>
  <action>
    Modify the handleListBusinesses function to add pagination support:
    - Parse query parameters: page (default 1), limit (default 20, max 100)
    - Calculate offset from page and limit
    - Modify SQL query to add LIMIT and OFFSET clauses
    - Add a COUNT query to get total number of businesses
    - Return response with structure: { data: [...], pagination: { page, limit, total, totalPages } }
    - Handle edge cases: negative page numbers, limit exceeding max, page beyond total
    - Maintain backward compatibility: if no pagination params provided, return all results in data array (no envelope)
  </action>
  <verify>Create test file and run: curl "http://localhost:8788/api/businesses?page=1&limit=5" -H "Accept: application/json"</verify>
  <done>GET /api/businesses?page=1&limit=5 returns paginated results with metadata</done>
</task>

<task type="auto">
  <name>Task 2: Test pagination edge cases</name>
  <files>test-pagination.sh</files>
  <action>
    Create a shell script to test pagination scenarios:
    - Test default pagination (no params)
    - Test specific page and limit
    - Test page=0 (should default to 1)
    - Test negative page (should default to 1)
    - Test limit exceeding max (should cap at 100)
    - Test page beyond total pages (should return empty data array)
    - Verify response structure includes pagination metadata

    Save test results to verify all cases handled correctly.
  </action>
  <verify>Run test script: bash test-pagination.sh</verify>
  <done>All pagination edge cases return valid responses</done>
</task>

</tasks>

<verification>
- GET /api/businesses returns unpaginated array for backward compatibility
- GET /api/businesses?page=1&limit=10 returns paginated envelope with metadata
- Pagination metadata includes: page, limit, total, totalPages
- Edge cases handled gracefully (invalid inputs use sensible defaults)
</verification>

<success_criteria>
- Business list endpoint supports optional pagination
- Response includes pagination metadata when params provided
- Backward compatibility maintained (no params = all results)
- Edge cases handled without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-apis/02-01-SUMMARY.md`
</output>