---
phase: 02-backend-apis
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [functions/api/[[path]].ts]
autonomous: true

must_haves:
  truths:
    - "POST /api/businesses validates all input fields"
    - "Duplicate slugs return 409 error"
    - "Invalid data returns 400 with specific error messages"
  artifacts:
    - path: "functions/api/[[path]].ts"
      provides: "Enhanced business validation"
      contains: "slug validation"
  key_links:
    - from: "POST /api/businesses"
      to: "validation logic"
      via: "field checks before DB insert"
      pattern: "body\\.(name|slug).*length"
---

<objective>
Enhance business creation validation and slug handling

Purpose: Ensure data integrity and provide clear error messages for invalid input
Output: Robust POST /api/businesses with comprehensive validation
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-apis/02-CONTEXT.md
@functions/api/[[path]].ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive field validation</name>
  <files>functions/api/[[path]].ts</files>
  <action>
    Enhance handleCreateBusiness function validation:
    - Validate name: required, 1-100 characters, trim whitespace
    - Validate slug: required, 1-50 characters, lowercase, alphanumeric with hyphens only (regex: /^[a-z0-9-]+$/)
    - Auto-generate slug from name if not provided: toLowerCase, replace spaces with hyphens, remove special chars
    - Validate place_id: optional, if provided must be non-empty string
    - Validate location: optional, max 200 characters
    - Validate description: optional, max 500 characters
    - Return specific error messages for each validation failure
    - Return 400 status code with { error: "Specific validation message" }
    - Ensure slug uniqueness is still enforced (existing 409 handling remains)
  </action>
  <verify>Test with curl: curl -X POST http://localhost:8788/api/businesses -H "Content-Type: application/json" -d '{"name":"Test!@#","slug":"invalid slug"}'</verify>
  <done>Invalid input returns 400 with specific validation error messages</done>
</task>

<task type="auto">
  <name>Task 2: Improve slug generation and conflict handling</name>
  <files>functions/api/[[path]].ts</files>
  <action>
    Add slug generation and conflict resolution:
    - If slug not provided, generate from name (lowercase, replace spaces/special chars with hyphens)
    - Clean up multiple consecutive hyphens to single hyphen
    - Remove leading/trailing hyphens
    - On slug conflict (409), check if auto-generated: append random 4-char suffix and retry once
    - If manually provided slug conflicts, return 409 with clear message
    - Add comment explaining slug generation rules

    Example transformations:
    - "Bob's Pizza!" → "bobs-pizza"
    - "Café München" → "cafe-munchen"
    - "ABC---123" → "abc-123"
  </action>
  <verify>Test slug generation: curl -X POST http://localhost:8788/api/businesses -H "Content-Type: application/json" -d '{"name":"Test Business #1"}'</verify>
  <done>Slugs auto-generate from names and handle conflicts gracefully</done>
</task>

</tasks>

<verification>
- POST /api/businesses with invalid name returns 400 with specific error
- POST /api/businesses with invalid slug format returns 400 with specific error
- POST /api/businesses without slug auto-generates valid slug from name
- POST /api/businesses with duplicate slug returns 409 error
- Field length validations enforced (name ≤100, location ≤200, description ≤500)
</verification>

<success_criteria>
- All input fields have proper validation
- Validation errors return 400 with specific messages
- Slugs auto-generate when not provided
- Duplicate slug handling works correctly
- Error messages are clear and actionable
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-apis/02-03-SUMMARY.md`
</output>