---
phase: 02-backend-apis
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All API endpoints respond correctly"
    - "Review generation produces authentic-looking text"
    - "Error cases handled gracefully"
  artifacts:
    - path: "functions/api/[[path]].ts"
      provides: "Complete API implementation"
      contains: "all required endpoints"
  key_links:
    - from: "API endpoints"
      to: "Cloudflare deployment"
      via: "Pages Functions"
      pattern: "onRequest.*export"
---

<objective>
Verify all API endpoints work correctly end-to-end

Purpose: Ensure the complete API surface is functional before frontend integration
Output: Confirmed working API with all requirements satisfied
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-apis/02-01-SUMMARY.md
@.planning/phases/02-backend-apis/02-02-SUMMARY.md
@.planning/phases/02-backend-apis/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete backend API implementation:
    - GET /api/businesses with pagination
    - POST /api/businesses with validation
    - GET /api/businesses/:slug
    - POST /api/generate-review with OpenRouter integration
  </what-built>
  <how-to-verify>
    1. Start local dev server: npm run dev
    2. Open new terminal for API testing

    Test business list with pagination:
    - curl "http://localhost:8788/api/businesses"
    - curl "http://localhost:8788/api/businesses?page=1&limit=2"
    - Verify: Returns businesses array or paginated envelope

    Test business creation:
    - curl -X POST http://localhost:8788/api/businesses \
      -H "Content-Type: application/json" \
      -d '{"name":"Test Restaurant","description":"Great food"}'
    - Verify: Returns created business with auto-generated slug

    Test invalid business creation:
    - curl -X POST http://localhost:8788/api/businesses \
      -H "Content-Type: application/json" \
      -d '{"name":""}'
    - Verify: Returns 400 with validation error

    Test get business by slug:
    - curl "http://localhost:8788/api/businesses/test-restaurant"
    - Verify: Returns specific business or 404

    Test review generation:
    - curl -X POST http://localhost:8788/api/generate-review \
      -H "Content-Type: application/json" \
      -d '{"businessName":"Test Restaurant","stars":5,"language":"en"}'
    - Verify: Returns { review: "..." } with generated text

    Test review generation with Romanian:
    - curl -X POST http://localhost:8788/api/generate-review \
      -H "Content-Type: application/json" \
      -d '{"businessName":"Restaurant Test","stars":4,"language":"ro"}'
    - Verify: Returns Romanian review text

    Deploy and test production:
    3. git add . && git commit -m "feat(02): implement backend APIs"
    4. git push origin main
    5. Wait for deployment (check Cloudflare dashboard)
    6. Test production endpoint:
       curl "https://reviewpasta.pages.dev/api/businesses"
  </how-to-verify>
  <resume-signal>Type "approved" when all endpoints work correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Phase 2 requirements verification:
- API-01: GET /api/businesses returns all businesses ✓
- API-02: GET /api/businesses/:slug returns business or 404 ✓
- API-03: POST /api/businesses validates input ✓
- API-04: POST /api/businesses enforces unique slugs ✓
- API-05: CORS headers present on all responses ✓
- API-06: POST /api/generate-review accepts required params ✓
- API-07: Review generation calls OpenRouter API ✓
- API-08: Returns generated review text ✓
- API-09: Handles errors gracefully with fallback ✓
</verification>

<success_criteria>
- All API endpoints return expected responses
- Pagination works with proper metadata
- Validation errors are clear and specific
- Review generation produces natural text
- Both English and Romanian languages work
- Production deployment successful
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-apis/02-04-SUMMARY.md`
</output>